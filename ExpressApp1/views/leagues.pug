extends layout.pug

block variables
  - var PageID = 'leagues';

block append style
  style.
    .fakeimg {
    height: 200px;
    background: #aaa;
    }

block content
 body
  p=l
  .container
   ul
     each league in leagues
       li= `${league.leagueName}`