doctype html
head
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0 ')
  link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css')
  title Login
  style
    include ../public/stylesheets/login.css

script(src="login.js")

  
.container
  .row.justify-content-center.align-items-center.inner-row
    .col-lg-5.col-md-7
      .form-box.login-form.p-5
        .form-title
          h2.fw-bold.mb-3 Login
        form(action='')
          .form-floating.mb-3
            input#floatingEmail.form-control.form-control-sm(type='email' placeholder='Email')
            label(for='floatingEmail') Email
          .form-floating.mb-3
            input#floatingPassword.form-control.form-control-sm(type='password' placeholder='Password')
            label(for='floatingPassword') Password
          .mt-3
            a.btn.btn-light.primaryBg(onclick='login()') Login
            
        .mt-3
          //ul 
            each user in users
              li= `${user.id}: ${user.email}`
          //let user = users.prepare(`SELECT * FROM users WHERE email = ?`).get("adam@ksu.edu")
          //p= user
          span Don't have account? 
          a.button(href='./signup') Sign Up
script.
  function login(){
  //alert(document.getElementsByTagName("input")[0].value)
  var email = document.getElementsByTagName("input")[0].value 
  var pass = document.getElementsByTagName("input")[1].value
  //var user = users.prepare("SELECT * FROM users WHERE id = ?").get(email)
  //alert(email)
  
  
  
  }
